using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using UnityEngine.EventSystems;

public class FriseScript : MonoBehaviour
{/*
    public GameObject frise;
    private Vector3 mousePosition;
    public float moveSpeed = 0.1f;
    // Start is called before the first frame update

    void Start()
    {
    }

    void Update()
    {
        
        Ray ray = Camera.main.ScreenPointToRay(Input.mousePosition);
        RaycastHit hit;
        if (Physics.Raycast(ray, out hit))
        {
            if (!EventSystem.current.IsPointerOverGameObject())
            {
                if (Input.GetMouseButtonDown(0) && hit.transform.name == "WorldMap" )
                {
                    enigme.SetActive(true);
                }
            }     
        }

        if (Input.GetMouseButton(0)) {

            GameObject text = EventSystem.current.currentSelectedGameObject;
            
            mousePosition = Input.mousePosition;
            mousePosition.z = 0f;
            text.transform.position = Camera.main.ScreenToWorldPoint(mousePosition);

            //transform.position = camera.main.ScreenToViewportPoint(Input.mousePosition);
        
    


    public void clicked()
    {
        boutonTrouver.GetComponent<Outline>().gameObject.SetActive(true);
        fleche.SetActive(true);
        message.SetActive(true);
    }

    public void closed()
    {
        frise.SetActive(false);
    }*/
}
